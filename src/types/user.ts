// define the shape of the platform user object
// extend as needed based on backend response
export interface IPlatFormUser {
  id: string;
  name: string;
  email: string;
  // add additional fields like roles, avatar, etc.
}
