// central place for string constants used as keys in localStorage, etc.
// add additional entries as needed by the application

export enum StorageKeys {
  token = 'token',
  user = 'user',
  shippingAddress = 'nupa-shipping-address',
  // add other keys here (e.g. theme = 'theme')
}
